============
Installation
============

At the command line::

    $ easy_install flask_pgbackup

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flask_pgbackup
    $ pip install flask_pgbackup
